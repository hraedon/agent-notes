---
identifier: '009'
kind: ux
severity: low
status: resolved
title: '`--workspace` unknown-name error is unhelpful'
---
## Resolution

Fixed in `src/agent_notes/cli/`. All inline workspace-not-found error messages
in `vocabulary.py` (3 locations), `breadcrumbs.py` (1), `export.py` (1), and
`links.py` (1) now list available workspaces, matching the central resolver in
`common.py`. JSON error responses also include an `"available"` array. CLI
tests pass (42/42).

## Observed (historical)

Passing `--workspace agent-notes-mcp` when only `default` exists prints `Workspace 'agent-notes-mcp' not found` and exits. The message should list the workspaces that *do* exist and hint at how to create one (or note that workspace names are project-scoped, not repo-scoped — easy mistake given the repo name matches no workspace).
