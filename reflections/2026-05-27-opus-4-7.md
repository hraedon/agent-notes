# Reflection — 2026-05-27 (Plan 004: opencode skill installer)

## Work done
- Implemented the opencode skill installer as part of Plan 004 (one of several tasks).
- `pytest tests/test_cli.py` is 19/19 green.
- Uncommitted blob also includes prior adversarial-review fixes (mixed into this session's diff — flagged below).

## Breadcrumb state
- Closed: none (no breadcrumbs were addressed this session).
- Opened:
  - **BC-008** (performance, medium): `agent-notes doctor` loads the 270MB embedding model on every invocation even when not needed.
  - **BC-009** (bug, low): `--workspace <name>` produces an unhelpful "not found" message when only `default` exists; should hint at available workspaces or fall back to `--path`.

## What remains
- Plan 004 has additional tasks beyond the opencode installer.
- The two filed breadcrumbs are independent of the installer and can be tackled separately.
- This commit mixes opencode-installer work with prior adversarial-review fixes — colleague should be told before merge so they can split if they prefer atomic history.

## Honesty notes
- Wrap-up was time-pressured (dinner at 7:00); breadcrumb bodies are accurate but terse.
- I did not verify whether BC-008 / BC-009 already existed as duplicates — under time pressure I filed first and skipped the dedup search. Low risk given empty `breadcrumbs/active/` before this session.
- AGENTS.md has no "Known issues" line, so the AGENTS.md sync step was skipped (not applicable, not avoided).
